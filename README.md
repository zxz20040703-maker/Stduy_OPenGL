Visual Studio的链接器原理：
  在配置项目依赖的时候和visual studio的Linker有所接触。是编译的最后一环，相当于把用到的dll、lib里的变量函数用占位符替代，在内存中找到对应位置后重新定向。

OpenGL
  虽然我的设备均是WIndows的OP，原生支持的图形api是DircX，但是几乎所有显卡厂家的原生驱动都是基于OpenGL规范的。所以掌握好OpenGL对后续的进步大有裨益。

后续学习文档同步到文件架
